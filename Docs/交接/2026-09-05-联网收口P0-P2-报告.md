# 执行报告：联网对战收口 Docs/23 P0→P2（2026-09-05 夜班）

> 读者：Codex / Codely / 用户，零上下文可读。基线链：Docs/25 → Docs/23 → Docs/24 v2。
> 结论：**P0/P1/P2 三阶段全部交付**，编译 0 错，EditMode 214 用例（新增 19 全绿），7 条失败全部为用户 WIP 存量（与本轮改动无关，见待决问题 §3）。等待用户实机验收（§5）。

## 0. 30 秒速览

- 执行者：zcode（GLM5.3flash 夜班）；授权：用户 2026-09-05 00:40 指令"不分阶段等待，完成全部内容+基础测试后等实机验收"；允许编译/EditMode 自主测试与子代理（本轮未用子代理，串行直做）。
- 交付：G1 换弹/切枪服务器验证 + G2 俯仰同步 + G3 伤害服务器权威门（P0）；G4 击杀竞赛玩法（P1）；G5 结算链路（P2）。
- 下一步：用户按 §5 手动清单实机验收；Codex/Codely 按 §6 复核指引 review。

## 1. 改动文件清单与提交记录

| 提交 | 内容 | 文件 |
|---|---|---|
| 63549332 | zcode 夜班协议 AGENTS.md（zcode 专属作用域） | AGENTS.md（新增） |
| 5620fa46 | **基线阻断修复**：Game.UI.asmdef 增加 FishNet.Runtime 引用 | Game.UI.asmdef |
| 0f5b54d5 | P0 权威战斗收口（G1+G2+G3） | MovementSimulationTypes.cs / DamageableTarget.cs / NetworkCombatAuthority.cs / NetworkWeaponState.cs / PlayerNetworkAdapter.cs / TPAimDriver.cs / WeaponHudView.cs + NetworkAuthorityGateTests.cs（新） |
| 9a17fe47 | P1 击杀竞赛（G4） | MatchRules.cs / MatchEventKind.cs / MatchLifecycle.cs（新）+ MatchHudView.cs（新，Presentation/HUD）+ NetworkCombatAuthority.cs / NetworkHud.cs / PlayerNetworkAdapter.cs 增量 + MatchLifecycleRulesTests.cs（新） |
| 2ee5da82 | P2 结算链路（G5） | AccountContracts.cs / MatchSettlementFlow.cs（新）/ LobbyPresenter.OverlayPages.cs（RenderResults 重写）+ MatchContractTests.cs（新） |

全部提交均逐文件点名 add，未卷入工作区任何用户未提交文件（每阶段后 `git status` 复核）。

### 实现要点（对应缺口）

- **G1**：`SubmitReloadRequest/ServerReloadRequest`、`SubmitSwitchRequest/ServerSwitchRequest`（反射读 Arsenal.slots，越界/空槽拒绝）；`NetworkWeaponState` 增 `_currentAmmo/_reserveAmmo` SyncVar（服务器写）；HUD 在线读权威值、离线回本地事件路径（F3 断线自动回退）。
- **G2**：`MovementCommand.PitchDelta`（系数与 yaw 同 0.1f）；服务器重放分支把俯仰写入 CameraPivot（`ApplyRemotePitch`，公式逐字复刻 FPMouseLook：Euler X 取负、±89° 夹紧）；`NetworkCombatAuthority._aimPitch` SyncVar 服务器采集（反射读 aimPivot，值变才写）；`AimDirectionWorld/IsOwnerPlayer` 访问器；`TPAimDriver` 远端玩家瞄准源切 `AimDirectionWorld`，本地路径逐字节保持。
- **G3**：`DamageableTarget.ShouldApplyDamage(networkActive, isServer)` 纯函数权威门（离线 true / 服务器 true / 纯客户端 false）；Owner 预测表现保留。
- **G4**：`MatchRules` 纯静态规则（20 杀/600s/3s 倒计时/8m 出生排除）；`MatchLifecycle` 服务器权威状态机；kills/deaths SyncVar + 击杀归因登记表；`OnMatchEvent` 中继 RPC；随机出生近邻排除 + 死亡/倒计时输入冻结；`MatchHudView` 纯代码 uGUI（比分条/血条/kill feed/Tab 比分板）；TP 前倾 85° 死亡表现（停动画写入者→倾倒，重生复位）。
- **G5**：请求契约去 `score` 增 `durationSeconds/isWin`；响应 DTO 13 成员镜像（passLevelUps/newAttachments/unlockedAchievements 等）；`MatchSettlementFlow` 终局收集全服务器权威值 → 提交 → 失败 PlayerPrefs 暂存（键 `pendingMatch:{clientMatchId}`）+ Results 重试按钮；`RenderResults` 重写为实数据（胜负/KD/三币/通行证/成就徽标）；断线不提交。

## 2. 新增测试（19 用例，全绿）

| 文件 | 用例数 | 覆盖 |
|---|---|---|
| NetworkAuthorityGateTests.cs | 6 | ShouldApplyDamage 三分支；PitchDelta 构造（字段完整性/夹紧语义/兼容构造器） |
| MatchLifecycleRulesTests.cs | 8 | EvaluateWinner 全分支（即时胜/超时比杀/比死/平局/双边 20 竞态）+ 四常量锁定 |
| MatchContractTests.cs | 5 | 请求 JSON 逐字对齐（无 score）；响应反序列化（后端形状 JSON）；预校验边界；暂存 PlayerPrefs 往返 |

测试基线与终态：开工基线 195 用例 188 绿 / 终态 **214 用例 207 绿**，7 条失败与基线完全一致（无新增失败）。注：弹药 SyncVar 语义（服务器写/Owner 读）与网络状态机推进依赖真实 FishNet 运行时，EditMode 无法模拟（测试程序集无 FishNet 引用），按票据约定跳过，由实机冒烟覆盖。

## 3. 待决问题清单

1. **用户 WIP 资产存在 YAML 解析错误**：`Day2_DemoBalance.asset` L131 "Expect ':' between key and value within mapping"（测试期间控制台实测）。该资产属用户未提交保护集，本轮未触碰；它是 Balance 缺失日志（rifle.02/handgun.03 等）与 CrosshairPulseAndBalanceTests 存量红的可能根因，建议用户整理 WIP 时优先检查。
2. **基线 7 红全为用户 WIP 存量**（非本轮引入）：CrosshairPulseAndBalanceTests ×3（Balance 缺失）、LobbySubPageTests.Catalog ×1（null）、LPWProductionAssetTests ×1（目录 45≠39）、ReloadAnimationTimingTests ×1（handgun.03）、FPWeaponAdsFireRoutingTests ×1（LPW Legacy 残留 8）。
3. **一次性提醒（Docs/24 §C 同款）**：工作区约 279 个用户未提交文件，建议用户自行整理提交，后续实施在干净基线进行；不整理不影响本轮可行性。
4. **服务器拒绝非法切枪的静默性**（设计内，复核关注）：ServerSwitchRequest 拒绝非法槽位时不产生 SyncVar 变化 → Owner 本地预测切枪会保留到下一次权威武器变化才被 HandleWeaponChanged 修正分支拉回。票据 §P0-2 即此设计；如需"即时回弹"需加拒绝回执 RPC（超出本轮范围）。

## 4. 与设计的偏差（实施适配 + 现场差异）

1. **【实施适配 1，按票据 E.1 采纳】**G2 用服务器侧 pivot 俯仰同步，未给 FireRequest 加方向参数；WeaponController 零改动（该文件有用户未提交改动，编辑即混提交事故）。
2. **【实施适配 2，按票据 F.0 采纳】**MatchLifecycle 为纯 MonoBehaviour，由 NetworkHud.Awake 运行时挂载（挂载对象已按 GUID 实地确认为 Arena 场景 `NetworkSystems`），广播经 NetworkCombatAuthority 中继；零资产改动。
3. **MovementCommand 构造器改为重载而非改签名**：Locomotor.cs L93 存在第三个构造点（B.1 第二组禁改文件），保留原 5 参构造器（PitchDelta 默认 0）+ 新增 6 参重载，Locomotor 零改动。
4. **Game.UI.asmdef 增加 "FishNet.Runtime" 引用**（5620fa46）：用户 WIP 文件 GameplayLoadoutBootstrap.cs 引用 FishNet 类型但程序集无引用 → 基线编译错误、EditMode 全阻。经用户会话内明确授权后修复（1 行）；用户 WIP 文件未动一个字节。该 asmdef 修改在 Docs/24 白名单外，已按"遇歧义即停"流程询问。
5. **MatchHudView 挂载经反射**：Gameplay 禁止引用 Presentation（三层单向依赖），NetworkCombatAuthority.OnStartClient 按项目反射惯例（同 WireRemotePresentation）按名调 `Game.Presentation.HUD.MatchHudView.TryMount`，try-null 兜底。
6. **MatchSettlementFlow 订阅点**：票据未指定订阅时机，采用 `[RuntimeInitializeOnLoadMethod]` 自动订阅（静态事件、幂等）。
7. **"返回大厅"不再 `SceneManager.LoadScene("Lobby")`**（票据 §G.2 原文）：结算流成功/失败路径均已先回 Lobby 场景再 Navigate(Results)，返回大厅按钮只需刷新档案+Navigate（重复 LoadScene 会把用户打回 Boot 页）。
8. **通行证刷新跳过**：Unity 侧 IApiClient 无 /api/pass 端点（通行证进度已内嵌 MatchResultDto）；补端点属范围外。
9. **kill feed 5 秒为硬移除**而非 alpha 渐隐（简式实现，票据"5 秒淡出"的近似）。
10. **结算自条目匹配按 (kills,deaths)**：MatchRules 保证非平局时双方条目必不相同、平局时双方 isWin 同 false，故匹配无歧义。
11. **新对局开始清零比分**：ServerStartCountdown 调 ServerResetScore（票据未明说；连续两局不清零会继承上局 kills 误触发终局）。
12. **编辑器桥启用**：Docs/24 原"执行者无 Unity 编辑器桥"假设被用户 2026-09-05 指令修订——本轮已执行编译检查 + EditMode 测试（遵守 Docs/07 卡死纪律：域重载窗口不发命令、refresh wait_for_ready 等）。
13. **Docs/23 §1.4 状态**：其"LPW 基线测试"护栏已按 Docs/25 §6 失效；本轮全程未触碰 LPW 资产/场景/代码。

## 5. 用户手动清单（实机验收）

```text
1. 打开 Unity 等编译：0 错误
2. Test Runner → EditMode 全跑：214 用例，除 §3.2 七条已知存量红外全绿
3. 【P1 资产清单·2 分钟】Arena → SpawnPoints 下复制 Spawn_0 三次 → 命名 Spawn_1/2/3
   → 互隔 >8m、不与 Spawn_0 重叠 → 保存场景
4. 双端实机 M 检查点（Docs/24 §J，LPFP 正式武器）：
   ①互看互跑与 TP 动画同步 ②开火命中一致（含远端俯/仰射——G2）
   ③击杀/比分/kill feed 实时同步 ④任一方 20 杀双端即时终局胜负一致
   ⑤10 分钟超时判定一致 ⑥死亡 3s 重生（非近邻出生点）、死亡期无法开火/移动
   ⑦结算提交双端各得三币、Results 实数据 ⑧重试/重放不重复发奖
   ⑨本地改弹药/血量作弊无效（G1/G3：Inspector 改客户端弹药后开火被服务器拒）
   ⑩返回大厅后档案/钱包刷新
5. 可选冒烟（单人离线零回归）：Arena 开火/换弹/切枪正常（离线语义不变）
```

## 6. 复核指引（写给 Codex/Codely）

建议重点核对（按风险降序）：

1. **PlayerNetworkAdapter.cs**：`ApplyRemotePitch` 与 FPMouseLook 的符号/夹紧一致性（服务器侧 pitch 复位的正确性全在此）；MovementFrozen 三分支（Host/远端重放/纯客户端）的命令丢弃语义。
2. **NetworkCombatAuthority.cs**：`ResolveAimPivot` 反射（远端玩家在服务器上无相机回退路径）；`ObserversMatchEvent` 的 `RunLocally=true`（host 自消费）是否符合 FishNet 4.7.2 语义；死亡表现对 Animancer/TPAnimDriver 的按名禁用是否覆盖实际组件类型。
3. **MatchLifecycle.cs**：`ServerEndMatch` 的 1v1 条目顺序假设（FindObjectsByType 顺序仅作 A/B 标号，胜负判定无序依赖）；`_relayHostStatic` 静态缓存的生命周期。
4. **MatchSettlementFlow.cs**：Ended 载荷自条目匹配（§4.10 的无歧义论证）；`ReturnToLobbyAndShowResultsAsync` 的 presenter 轮询（600 帧上限）时序假设——LobbyBootstrap.Start 中 AddComponent 与 Initialize 同步完成。
5. **AccountContracts.cs**：响应 DTO 与后端 `Contracts.cs` L89-94 逐字比对（本轮按当时读到的字段实现；后端若后续改字段需重对齐）。
6. **审计抽样**：`git show --stat 0f5b54d5 9a17fe47 2ee5da82` + 每提交 `git status` 未卷入用户未提交文件（每阶段交付时已逐次核验）。
